const number = 50
let ressult

if  (number >= 50) {
	result = 'Not to fifty!'
} else {
	result = 'It coud ber worse'
}

console.log(result)


number >= 50 ? 'Not to  fifty' : 'It could be worse' 
rez = number >= 50 ? 'Not to  fifty' : 'It could be worse' // and we can set rez 
// this is great because we can save a lot of time just writing one line
console.log(rez)

